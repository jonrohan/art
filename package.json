{
  "name": "art",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "run-p start-compile start-run",
    "start-compile": "tsc --watch",
    "start-run": "browser-sync start --server -w"
  },
  "dependencies": {
    "@types/p5": "^1.7.6",
    "browser-sync": "^3.0.2",
    "npm-run-all": "^4.1.5",
    "p5": "^1.9.0",
    "typescript": "^5.3.3"
  }
}
